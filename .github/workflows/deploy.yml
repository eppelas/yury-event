name: Deprecated Pages Workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Pages deploy now lives in .github/workflows/deploy-pages.yml"
